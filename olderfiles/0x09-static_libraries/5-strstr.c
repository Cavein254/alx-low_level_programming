#include <stdio.h>
/** _strstr - return set of strings
 * @haystack : numerous strings
 * @needle : the string being found
 * Return: 0 on success
*/

char *_strstr(char *haystack, char *needle)
{
    return (0);
}