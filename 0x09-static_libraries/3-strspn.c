#include <stdio.h>
/** _strspn - set the memory two strings
 * @s : origin string
 * @accept : additional chars
 * Return: 0 on success
*/
unsigned int _strspn(char *s, char *accept)
{
    return (0);
}