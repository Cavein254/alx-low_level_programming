#include <stdio.h>
/** _memset - set the memory two strings
 * @s : origin string
 * @b : copy of the string
 * @n : integer
 * Return: 0 on success
*/
char *_memset(char *s, char b, unsigned int n)
{
    return (0);
}